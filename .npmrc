; react-paystack@6 declares a peer range of react ^15–^18 and has not been
; updated for React 19, which this project runs. The package works fine at
; runtime; only the declared range is stale. node_modules was already installed
; this way — recording it here makes installs reproducible instead of relying
; on someone remembering to pass the flag.
legacy-peer-deps=true
